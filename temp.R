
score<-seq(0,1,0.01)

df.tresholds<-data.frame(treshold=score,
               TP=0,
               FP=0,
               TN=0,
               FN=0,
               ACC=0,
               Gain=0
               )

for i in (score) {
  
}

